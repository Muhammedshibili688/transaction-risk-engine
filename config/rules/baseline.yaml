experiment_name: "Heuristic_Rule"
rule_version: "v1.0"

thresholds:
  geo_speed_limit: 800
  amount_ratio_limit: 2.5
  switch_count_limit: 5

weights:
  # identity signals
  new_device: 40
  new_ip: 5
  country_change: 25

  # behavioral signals (used as base multipliers)
  impossible_travel: 40
  high_risk_merchant: 10